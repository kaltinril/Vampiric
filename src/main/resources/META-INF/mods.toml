modLoader="javafml" #mandatory
loaderVersion="[30,)" #mandatory (28 is current forge version)
issueTrackerURL="https://www.youtube.com/user/Kaltinril" #optional

# A list of mods - how many allowed here is determined by the individual mod loader
[[mods]] #mandatory
modId="vampiric" #mandatory
version="0.1.0" #mandatory
displayName="Vampric" #mandatory
# updateJSONURL="http://myurl.me/" #optional
displayURL="https://www.youtube.com/user/Kaltinril" #optional
logoFile="vampiric.png" #optional # A file name (in the root of the mod JAR) containing a logo for display
credits="Thanks for assistance from Cadiboo.  Youtube channels: Hary Talks, TurtyWurty, Champion Ash5357, Jorrit Tyberghein, TechnoVision, AlanBeckerMinecraft, wuppygaming, "
authors="Kaltinril" #optional
description='''
A mod that adds Classic Monsters like the mythical creatures vampire, werewold, and mummy.
'''

# A dependency - use the . to indicate dependency for a specific modid. Dependencies are optional.
[[dependencies.vampiric]] #optional
    # the modid of the dependency
    modId="forge" #mandatory
    # Does this dependency have to exist - if not, ordering below must be specified
    mandatory=true #mandatory
    # The version range of the dependency
    versionRange="[30,)" #mandatory
    # An ordering relationship for the dependency - BEFORE or AFTER required if the relationship is not mandatory
    ordering="NONE"
    # Side this dependency is applied on - BOTH, CLIENT or SERVER
    side="BOTH"

# Here's another dependency
[[dependencies.vampiric]]
    modId="minecraft"
    mandatory=true
    versionRange="[1.15.2]"
    ordering="NONE"
    side="BOTH"
